/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'drive.google.com', // Replace with your specific Google Drive subdomain if necessary
        ],
    },
};

export default nextConfig;
